import { describe, expect, it } from "@jest/globals"

describe("ResetPasswordForm", () => {
  it("should do something", () => {
    const brevity = true
    const it = true
    const is = true
    const correct = true
    const and = true

    expect(brevity && it && is && correct && and).toBe(true)
  })
})

